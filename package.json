{
  "name": "mqtt-relay",
  "version": "1.0.0",
  "description": "A small relay app that receives MQTT packets and transforms them into HTTP requests and vice versa.",
  "main": "src/index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development PORT=8080 nodemon src/index.js",
    "start:prod": "NODE_ENV=production node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mqtt",
    "relay",
    "http"
  ],
  "engines": {
    "node": "10.10.0"
  },
  "author": "pineconesundae",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "mqtt": "^4.1.0",
    "nodemon": "^2.0.4"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}